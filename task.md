# Fix Scrolling Behavior on Continue

- [ ] Stop the user from seeing the initial learning card (first page) repeatedly when transitioning to new pop quizzes.
- [ ] Implement an effect that automatically scrolls the newly rendered `.quiz-container` clearly into view whenever `currentStep` advances, ensuring the user focuses on the new question rather than feeling like the page restarted.
